#include <map>
#include <set>
#include <list>
#include <cmath>
#include <ctime>
#include <deque>
#include <queue>
#include <stack>
#include <string>
#include <bitset>
#include <cstdio>
#include <limits>
#include <vector>
#include <climits>
#include <cstring>
#include <cstdlib>
#include <fstream>
#include <numeric>
#include <sstream>
#include <iostream>
#include <algorithm>
#include <unordered_map>

using namespace std;


int main(){
    int d1,d2,l,x,y;
    l = 12;
    cin>>d1;
    cin>>d2;
    c
    y = d2*sin(arcos((pow(d2,2)+pow(l,2)-pow(d1,2))/(2*l*d2)));
    if (d2>=d1){
    x = -((pow(d2,2)+pow(l,2)-pow(d1,2))/(2*l))+9;}
    else{
    x = -((pow(d2,2)+pow(l,2)-pow(d1,2))/(2*l))-3;}
    cout<<x;
    cout<<y;
    return 0;
}

